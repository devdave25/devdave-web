export enum ModalType {
  Base64Encode,
  UrlEncode,
  HtmlEncode,
  Uuid,
  JsonViewer,
  JsonValidator,
  XmlViewer,
  XmlValidator,
  ColourConverter,
  ColourGenerator,
  ImageConverter,
  ImageResizer
}
